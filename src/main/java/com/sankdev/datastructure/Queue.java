package com.sankdev.datastructure;

/**
 * ADT that represents a collection of elements that support operations: add(x) and remove().
 * <br> Note: Queue's queueing discipline decides which element should be removed:
 * <br> FIFO (e.g.: a line-up at a cash register in a grocery store). Regular Queue that supports
 * enqueue(x)/dequeue() methods;
 * <br> LIFO (e.g.: a stack of plates). See Stack interface;
 * <br> priority (e.g.: a triage of patients in a hospital emergency room). Supports
 * add(x)/removeMin() methods.
 */
public interface Queue {

}
